# Copy to .env. VITE_* values are exposed to the browser — no secrets here.
# API calls use /api (proxied to the backend in vite.config.js).

VITE_API_BASE=/api
VITE_BACKEND_URL=http://127.0.0.1:5001
