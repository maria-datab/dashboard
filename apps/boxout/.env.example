# Copy to .env. VITE_* values are exposed to the browser — no secrets here.
# Restart `npm run dev` after changes.

VITE_API_BASE=/api/app
VITE_BACKEND_URL=http://127.0.0.1:5000
VITE_POLL_INTERVAL_MS=1200
